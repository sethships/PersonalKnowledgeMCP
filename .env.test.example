# Personal Knowledge MCP - Test Environment Configuration
# Copy this file to .env.test and update the values for auth-enabled testing
#
# IMPORTANT: Do not commit .env.test to version control

# =============================================================================
# ChromaDB Authentication Test Token
# =============================================================================
# This token is used for the auth-enabled ChromaDB test instance
# Generate a secure random token for testing, e.g.:
#   openssl rand -hex 32
#
CHROMADB_TEST_AUTH_TOKEN=test-token-change-me-in-production

# =============================================================================
# Auth Test ChromaDB Connection Settings
# =============================================================================
# These settings point to the auth-enabled test ChromaDB instance
# Port 8100 is used by docker-compose.test.yml
#
CHROMADB_AUTH_TEST_HOST=localhost
CHROMADB_AUTH_TEST_PORT=8100

# =============================================================================
# Test Environment Flags
# =============================================================================
# Set to 'true' to run auth-enabled integration tests
# Tests will be skipped if this is not set or false
#
RUN_AUTH_INTEGRATION_TESTS=true
